---
title: Momentum Trading ML API
emoji: chart_with_upwards_trend
colorFrom: green
colorTo: blue
sdk: docker
app_port: 7860
---

# Momentum Trading ML API

FastAPI backend for the Momentum Trading ML dashboard.

This Docker Space uses the lightweight `requirements-api.txt` dependency set.

Health check:

```text
/api/health
```

Live watchlist endpoint:

```text
/api/custom-run
```
